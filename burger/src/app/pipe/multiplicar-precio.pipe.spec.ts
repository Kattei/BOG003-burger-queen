import { MultiplicarPrecioPipe } from './multiplicar-precio.pipe';

describe('MultiplicarPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicarPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
